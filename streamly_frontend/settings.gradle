pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// Primary Gradle root for the Android app
rootProject.name = "streamly-frontend"

// Include all modules for proper task discovery (clean, assemble, etc.)
include(":app")
include(":list")
include(":utilities")
