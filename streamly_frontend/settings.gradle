 // PUBLIC_INTERFACE
 /**
  * Conventional Groovy settings at the Android project root for tool compatibility.
  * Mirrors settings.gradle.kts and includes all modules.
  */
 pluginManagement {
     repositories {
         google()
         gradlePluginPortal()
         mavenCentral()
     }
     plugins {
         id("com.android.application") version "8.6.1"
         id("com.android.library") version "8.6.1"
         id("org.jetbrains.kotlin.android") version "2.0.21"
     }
 }

 dependencyResolutionManagement {
     repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
     repositories {
         google()
         mavenCentral()
         gradlePluginPortal()
     }
 }

 rootProject.name = "example-android-app"

 include(":app")
 include(":list")
 include(":utilities")
