pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// Root shim: the actual Android project is under streamly-tv-streaming-app-96894/streamly_frontend/
rootProject.name = "streamly-root-delegate"

// Forward all Gradle invocations to the real project via composite build
includeBuild("streamly-tv-streaming-app-96894/streamly_frontend")
