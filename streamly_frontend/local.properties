# PUBLIC_INTERFACE
# Template local.properties for Android SDK location. Do not commit secrets.
# The CI environment should set ANDROID_HOME / ANDROID_SDK_ROOT instead.
# sdk.dir=/path/to/Android/sdk
