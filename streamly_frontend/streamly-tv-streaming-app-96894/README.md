// PUBLIC_INTERFACE
/**
 * Repository root README.
 *
 * Build instructions:
 * - The Android project resides in streamly_frontend/.
 * - Run Gradle from that directory:
 *     cd streamly_frontend && ./gradlew :app:assembleDebug
 * - Or from the repository root using the composite build:
 *     ./gradlew -p streamly_frontend :app:assembleDebug
 */
