# PUBLIC_INTERFACE
# Wrapper directory placeholder at repository root
This directory exists to satisfy tools that detect the Gradle project root by checking for a `gradle/wrapper/` path.
The actual Gradle wrapper used for builds lives under `streamly_frontend/gradle/wrapper/`.
