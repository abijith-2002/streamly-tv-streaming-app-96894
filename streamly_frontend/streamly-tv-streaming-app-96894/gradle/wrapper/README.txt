This directory contains the Gradle wrapper configuration for the repository root shim.
The canonical Gradle wrapper lives under streamly_frontend/gradle/wrapper/.
Tools that start at repository root can use this wrapper metadata to locate the Gradle distribution, then the root gradlew script delegates into streamly_frontend.
