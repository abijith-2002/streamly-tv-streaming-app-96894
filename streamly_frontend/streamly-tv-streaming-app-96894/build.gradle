/*
 Redirect shim: Points analyzers to the canonical Gradle root one level up.
*/
tasks.register("helpRedirect") {
    doLast {
        println("This is a redirect shim. Use ../gradlew from streamly_frontend (canonical root).")
    }
}
