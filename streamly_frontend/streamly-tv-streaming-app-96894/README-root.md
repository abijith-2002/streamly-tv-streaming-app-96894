// PUBLIC_INTERFACE
/**
 * Root-level README
 *
 * Build the Android app:
 * - From repository root:
 *     ./gradlew :app:assembleDebug
 * - Or change directory and run:
 *     cd streamly_frontend && ./gradlew :app:assembleDebug
 *
 * The actual Android project root is in `streamly_frontend/`.
 * The root `settings.gradle` includes it via composite build.
 */
